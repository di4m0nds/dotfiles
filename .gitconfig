[user]
	name = di4m0nds

[color]
	ui = true
  status = auto
  branch = auto
  interactive = auto

[alias]
  a = add
  d = diff
  lg = "log --all --oneline -5 --decorate --graph"
  co = checkout
  ci = commit
  st = status
  br = branch
  ps = "!git push origin $(git rev-parse --abbrev-ref HEAD)"
  pl = "!git pull origin $(git rev-parse --abbrev-ref HEAD)"
